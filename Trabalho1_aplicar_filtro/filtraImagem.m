## This file is not part of Octave.## -*- texinfo -*-## @deftypefn {Function File} [@var{alpha}] = }filtraImagem (@var{pathArq}, @var{linhaMatriz}, @var{colunaMatriz}, @var{qtdFiltro})## Filtra Imagens, possivelmente, funciona melhor com imagens que não tenham passado pelo filtro rgb2gray## A barra para inserir o caminho no pathArq é " / "## a qtdFiltro é para ser x/y e não x.y## Aluna Jacqueline Abreu - 2011.1## @end deftypefnfunction filtraImagem(pathImage, linhaFiltro, colunaFiltro, qtdFil)	img=imread(pathImage);        cinza = isgray(img);        if(cinza==false)        img=rgb2gray(img);    endif        img2=img;	        fil=ones(linhaFiltro,colunaFiltro)*1/(linhaFiltro*colunaFiltro);	        for j = 1:5            img2 = filter2(fil, img2);        endfor	        img2 = uint8(img2);	        imgFinal= img - (qtdFil *img2);	        imshow(imgFinal);endfunction
